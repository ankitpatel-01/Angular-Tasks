import { BoolenConverterPipe } from './boolen-converter.pipe';

describe('BoolenConverterPipe', () => {
  it('create an instance', () => {
    const pipe = new BoolenConverterPipe();
    expect(pipe).toBeTruthy();
  });
});
